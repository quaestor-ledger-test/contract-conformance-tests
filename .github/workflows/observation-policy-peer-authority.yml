name: Quaestor observation peer-authority convergence

on:
  pull_request:
    paths:
      - 'contracts/observation-policy/**'
      - '.github/workflows/observation-policy-peer-authority.yml'
  push:
    branches: [main]
    paths:
      - 'contracts/observation-policy/**'
      - '.github/workflows/observation-policy-peer-authority.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  peer-authority:
    uses: flags-2-env-test/contract-conformance-tests/.github/workflows/peer-authority-convergence.yml@7756eb9b08ec4909c4acb8ed3cef59f4280028bd
    with:
      typespec_path: contracts/observation-policy/main.tsp
      schema_path: contracts/observation-policy/authored.schema.json
      instances_path: contracts/observation-policy/instances
      expected_declarations_json: '["QuaestorLedgerObservation.ObservationPolicyV1"]'
